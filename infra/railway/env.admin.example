# Admin web — Railway service "admin-web"
NODE_ENV=production
NIXPACKS_CONFIG_FILE=nixpacks.admin.toml

# Admin BFF proxies to the API with the user's bearer token
NEXT_PUBLIC_API_BASE_URL=https://api.brand2school.co.za
