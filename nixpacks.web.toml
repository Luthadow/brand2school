# Railway **web** service only. Set variable: NIXPACKS_CONFIG_FILE=nixpacks.web.toml

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build -w @brand2school/web"]

[start]
cmd = "npm run start -w @brand2school/web"

[variables]
NPM_CONFIG_PRODUCTION = "false"
