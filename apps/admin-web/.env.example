NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# Must match INTERNAL_API_KEY in apps/api/.env (server-only — never NEXT_PUBLIC)
B2S_INTERNAL_API_KEY=replace_me_internal_key_min_16_chars
# Must match ADMIN_WEB_APP_URL in apps/api/.env (used in admin password-reset emails)
# Local dev: http://localhost:3001 — production: https://admin.brand2school.co.za
ADMIN_WEB_APP_URL=http://localhost:3001
